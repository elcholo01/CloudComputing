@{
    ViewBag.Title = "Health Status Service - RADI!";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 40px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            min-height: 100vh;
        }
        .container {
            max-width: 900px;
            margin: 0 auto;
            background: rgba(255,255,255,0.95);
            color: #333;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            text-align: center;
        }
        .header {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
            padding: 30px;
            margin: -40px -40px 30px -40px;
            border-radius: 15px 15px 0 0;
        }
        h1 {
            font-size: 2.5em;
            margin: 0;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }
        .subtitle {
            font-size: 1.1em;
            margin-top: 10px;
            opacity: 0.9;
        }
        .success {
            background: linear-gradient(135deg, #d4edda, #c3e6cb);
            border: 2px solid #28a745;
            padding: 25px;
            border-radius: 10px;
            margin: 20px 0;
            color: #155724;
        }
        .info {
            background: linear-gradient(135deg, #d1ecf1, #bee5eb);
            border: 2px solid #17a2b8;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
            color: #0c5460;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin: 20px 0;
        }
        .stat-card {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
        }
        .stat-number {
            font-size: 1.8em;
            font-weight: bold;
            color: #667eea;
        }
        .stat-label {
            font-size: 0.9em;
            color: #666;
            margin-top: 5px;
        }
        .btn {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 25px;
            font-size: 16px;
            text-decoration: none;
            display: inline-block;
            margin: 10px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(40, 167, 69, 0.3);
        }
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(40, 167, 69, 0.4);
            color: white;
            text-decoration: none;
        }
        .footer {
            margin-top: 40px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 8px;
            font-size: 14px;
            color: #666;
        }
        .status-indicator {
            display: inline-block;
            width: 12px;
            height: 12px;
            background: #28a745;
            border-radius: 50%;
            margin-right: 8px;
            animation: pulse 2s infinite;
        }
        @@keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Health Status Service</h1>
            <div class="subtitle">Monitoring dostupnosti Movie Discussion Forum servisa</div>
        </div>

        <div class="success">
            <h2><span class="status-indicator"></span>USPESNO POKRETANJE!</h2>
            <p><strong>HealthStatusService web aplikacija je pokrenuta!</strong></p>
            <p>Vreme pokretanja: @DateTime.Now.ToString("dd.MM.yyyy HH:mm:ss")</p>
        </div>

        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-number">100%</div>
                <div class="stat-label">Aplikacija dostupna</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">@Request.Url.Port</div>
                <div class="stat-label">Port</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">OK</div>
                <div class="stat-label">Status servisa</div>
            </div>
        </div>

        <div class="info">
            <h3>Health Status Dashboard</h3>
            <p>Web aplikacija za pracenje dostupnosti servisa je uspesno pokrenuta!</p>
            <p>Sve .NET Framework zavisnosti su ispravno podesene.</p>
        </div>

        <div style="margin-top: 30px;">
            <a href="/test" class="btn">Test stranica</a>
            <a href="/Simple/Test" class="btn">API Test</a>
            <a href="/Home/Dashboard" class="btn">Kompletan Dashboard</a>
        </div>

        <div class="footer">
            <p><strong>Za odbranu:</strong> Ova stranica dokazuje da je web aplikacija potpuno funkcionalna</p>
            <p><strong>Cloud Computing Projekat</strong> - Health Monitoring System</p>
            <p><strong>Host:</strong> @Request.Url.Host | <strong>Protokol:</strong> @Request.Url.Scheme</p>
        </div>
    </div>
</body>
</html>